<!DOCTYPE html>
<html>
    <head>
        <title>Country Escapes Inc.</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
        <link rel="stylesheet" href="/css/home.css">
        <script src="https://cdnjs.cloudflare.com/ajax/libs/modernizr/2.8.3/modernizr.min.js" type="text/javascript" async></script>
        <!-- async allows modernizer to be loaded simultaneously alongside the other assets on the page-->
    </head>
    <body>

 <div class="container">
        <% if(error && error.length > 0){ %>
            <div class="alert alert-danger" role="alert">
                <%= error %>
            </div>
        <% } %>
        <% if(successLog && successLog.length > 0){ %>
            <div class="alert alert-success" role="alert">
                <%= successLog %>
            </div>
        <% } %>
    </div>

    <div id="landing-header">
 		<h1>Welcome to Country Escapes Inc.</h1>
		<a href="/campgrounds" class="btn btn-lg btn-success">View All Locales</a>
		      <p>
                         <form action='/campgrounds' method='GET' class='form-inline'>
                              <div class='form-group'>
                                   <input type='text' name='search' placeholder='Enter search term here' class='form-control'>
                                   <input type='submit' value='search' class='btn btn-default'>
                              </div>
                         </form>
                    </p>
    </div>
    
    <ul class="slideshow">
      <li></li>
      <li></li>
      <li></li>
      <li></li>
      <li></li>
    </ul>

<% include partials/footer %>